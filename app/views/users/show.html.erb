<div class="container">
  <!-- USER_INFORMATION SECTION -->
  <div class="user-info">
    <div class="user-form">
      <div class="user-top">
        <div class="user-icon">
          <% if current_user.image? %>
            <%= image_tag current_user.image.url %>
          <% else %>
            <%= image_tag "default.png", class: "guest-user" %>
          <% end %>
        </div>
        <% if current_user.name? %>
          <div class="user-name">
            <h2><%= current_user.name %></h2>
          </div>
        <% else %>
          <div class="user-name">
            <h2>ゲスト</h2>
          </div>
        <% end %>
        <div class="user-gender">
          <% case current_user.gender %>
            <% when "0" %>
              <span class="fa-solid fa-person man"></span>
            <% when "1" %>
            <span class="fa-solid fa-person-dress woman"></span>
          <% end %>
        </div>
      </div>
      <div class="user-down">
        <% if current_user.introduction? %>
          <div class="user-introduction">
            <%= current_user.introduction %>
          </div>
        <% else %>
          <div class="user-introduction-exmple">
            <p>例) 初めまして、20代後半で2児のパパです。<br>
            子育てに悩むことも多くいろんな方のご意見などを参考にしたいと思っております。<br>
            現在は、上の子がもう少しで小学校に入学するので子供の教育について興味があります。<br>
            よろしくお願いします。</p>
          </div>
        <% end %>
      </div>
      <% if current_user.name? %>
        <div class="user-links">
          <%= link_to "プロフィール編集", edit_user_path, class: "user-edit" %>
          <%= link_to "アカウント編集", edit_user_registration_path, class: "user-edit" %>
        </div>
      <% end %>
    </div>
  </div>
  <!-- USERS_INFORMATION SECTION -->

  <!-- USERS_POSTS SECTION -->
  <h1>POST</h1>
    <div class="post-content container-fluid">
      <% @posts.each do | post | %>
      <div class="post-user-content row">
        <div class="post-content-side col-md-2">
          <% if @user.image? %>
            <div class="user-icon">
              <%= image_tag @user.image.url %>
            </div>
          <% else %>
            <div class="user-icon">
              <%= image_tag "default.png" %>
            </div>
          <% end %>
        </div>
        <div class="post-content-main col-md-10">
          <div class="post-content-main-top">
            <p class="post-user-name"><%= @user.name %></p>
            <p class="post-user-create-time"><%= post.created_at.to_s(:datetime_jp) %></p>
          </div>
          <div class="post-content-main-message">
            <%= post.content %>
          </div>
          <% if post %>
          <div class="post-down">
            <%= link_to edit_post_path do %>
              <i class="fa-regular fa-pen-to-square"></i>
            <% end %>
            <%= link_to post_path(current_user.id), method: :delete do %>
              <i class="fa-regular fa-trash-can"></i>
            <% end %>
          </div>
          <% end %>
        </div>
      </div>
      <% end %>
    </div>
  <!-- USERS_POSTS SECTION -->
</div>
